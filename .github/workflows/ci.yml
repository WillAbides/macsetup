name: ci
on: [ push, workflow_dispatch ]
jobs:
  setup:
    name: setup
    runs-on: macos-11
    steps:
      - uses: actions/checkout@v2
      - run: |
          brew --prefix
          brew uninstall go@1.15
      - run: |
          ./setup.sh
